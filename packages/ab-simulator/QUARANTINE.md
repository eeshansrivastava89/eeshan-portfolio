# Quarantined — Pending Migration

This package is **non-functional** in the current repo. It still references the deleted `@shared` package and will not build.

It is preserved here solely as a reference for the standalone migration. See `internal/docs/AB_SIMULATOR_MIGRATION.md` for the full migration plan.

**Do not attempt to build this package.** It will be deleted once the migration to a standalone repo is complete.
